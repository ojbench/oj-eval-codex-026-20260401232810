#include <iostream>
#include "calc.h"

int main() {
    // This main is a placeholder to satisfy the build.
    // The online judge will compile and use its own harness.
    std::cout << "OK";
    return 0;
}
